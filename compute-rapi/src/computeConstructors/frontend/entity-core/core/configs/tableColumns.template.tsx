import { ColumnConfig } from '@ps/shared-core/ui/DataTableV2/TableColumns';
import { formatDate, formatDateTime } from '@ps/shared-core/config/dateUtils';
import { getRootDomainClient } from '@ps/shared-core/config/generalUtils';
// @gen:IMPORTS

const columns: ColumnConfig[] = [
  // @gen:COLUMNS
];

export default columns;
